{
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "npm run --prefix frontend build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://healthapp-b4mo.onrender.com/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
