{
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "npm run --prefix frontend build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://healthapp-b4mo.onrender.com/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
